{
  "name": "notes-app",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "api",
    "app"
  ],
  "engines": {
    "npm": "9.2.0"
  },
  "main": "index.js",
  "scripts": {
    "build" :"SKIP_PREFLIGHTCHECK=true npm run build --workspace=app ",
    "start": "npm run start --workspace=api",
    "dev:api": "npm run build && npm run  dev --workspace=api"
  },
  "eslintConfig": {
    "extends": "./node_modules/standard/eslintrc.json",
    "env": {
      "cypress/globals": true,
      "jest": true
    },
    "plugins": [
      "cypress"
    ]
  },
  "devDependencies": {
    "standard": "17.0.0",
    "eslint-plugin-cypress": "2.12.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}